const PT_TO_PX = 96 / 72;
export function ptToPx(pt) {
    return pt * PT_TO_PX;
}
export function pxToPt(px) {
    return px / PT_TO_PX;
}
export function calculateScale(slideWidth, slideHeight, canvasWidth, canvasHeight) {
    const scaleX = canvasWidth / ptToPx(slideWidth);
    const scaleY = canvasHeight / ptToPx(slideHeight);
    return Math.min(scaleX, scaleY);
}
//# sourceMappingURL=coordinates.js.map